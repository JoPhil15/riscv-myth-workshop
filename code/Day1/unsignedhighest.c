#include<stdio.h> 
#include<math.h> 
int main() 
{//highest no that can be rep = 2^64 
unsigned long long int max = (unsigned ong long int) (pow(2,64) -1);//datatype for double word 
printf("HIghesr number represented by unsigned long long int is %llu \n", max); 
return 0;
